import { State } from './types';

export default {
  filter: {
    date: Date.now(),
    magnitude: 10,
    latitude: 47,
    longitude: 19,
    twilight: -18,
    altitude: 20,
    moonless: true
  }
} as State;
